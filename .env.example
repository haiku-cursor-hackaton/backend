SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
PUBLIC_BASE_URL=http://127.0.0.1:8000
# Produccion: https://genko-platform-production.up.railway.app
MCP_PATH=/mcp
ENVIRONMENT=development
# Origenes extra permitidos por CORS (separados por coma). Los localhost y el
# dominio de Netlify ya vienen por defecto; añade aqui otros dominios si hace falta.
CORS_ORIGINS=
